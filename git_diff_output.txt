diff --git a/download_images.js b/download_images.js
deleted file mode 100644
index 03baa89..0000000
--- a/download_images.js
+++ /dev/null
@@ -1,54 +0,0 @@
-/* eslint-disable */
-const fs = require('fs');
-const https = require('https');
-const path = require('path');
-
-const publicImagesDir = path.join(__dirname, 'public', 'images');
-
-if (!fs.existsSync(publicImagesDir)){
-    fs.mkdirSync(publicImagesDir, { recursive: true });
-}
-
-const imagesToDownload = [
-    { url: 'https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?auto=format&fit=crop&w=1600&q=80', filename: 'kyoto.jpg' },
-    { url: 'https://images.unsplash.com/photo-1559103986-eab52cb10263?auto=format&fit=crop&w=1600&q=80', filename: 'lake-como.jpg' },
-    { url: 'https://images.unsplash.com/photo-1643900223789-5f252cfb2a26?auto=format&fit=crop&w=1600&q=80', filename: 'cappadocia.jpg' },
-    { url: 'https://images.unsplash.com/photo-1523365280197-f1c54817a353?auto=format&fit=crop&w=1600&q=80', filename: 'tuscany.jpg' },
-    { url: 'https://images.unsplash.com/photo-1530122037265-a5f1f91d3b99?auto=format&fit=crop&w=1600&q=80', filename: 'swiss-alps.jpg' },
-    
-    { url: 'https://images.unsplash.com/photo-1506377585622-bedcbb027afc?auto=format&fit=crop&w=1400&q=80', filename: 'journey-scotland.jpg' },
-    { url: 'https://images.unsplash.com/photo-1486911278844-a81c5267e227?auto=format&fit=crop&w=1400&q=80', filename: 'journey-fife.jpg' },
-    { url: 'https://images.unsplash.com/photo-1602216056096-3b40cc0c9944?auto=format&fit=crop&w=1400&q=80', filename: 'journey-crail.jpg' },
-
-    { url: 'https://images.unsplash.com/photo-1506377247377-2a5b3b417ebb?auto=format&fit=crop&w=1200&q=80', filename: 'how-main.jpg' },
-    { url: 'https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&w=800&q=80', filename: 'how-small.jpg' }
-];
-
-function downloadImage(url, filepath) {
-    return new Promise((resolve, reject) => {
-        https.get(url, (res) => {
-            if (res.statusCode === 200) {
-                res.pipe(fs.createWriteStream(filepath))
-                   .on('error', reject)
-                   .once('close', () => resolve(filepath));
-            } else {
-                res.resume();
-                reject(new Error(`Request Failed With a Status Code: ${res.statusCode}`));
-            }
-        }).on('error', reject);
-    });
-}
-
-async function run() {
-    for (const img of imagesToDownload) {
-        const filepath = path.join(publicImagesDir, img.filename);
-        try {
-            await downloadImage(img.url, filepath);
-            console.log(`Downloaded ${img.filename}`);
-        } catch (e) {
-            console.error(`Error downloading ${img.filename}: ${e.message}`);
-        }
-    }
-}
-
-run();
diff --git a/fix_images.js b/fix_images.js
deleted file mode 100644
index db2773d..0000000
--- a/fix_images.js
+++ /dev/null
@@ -1,31 +0,0 @@
-const fs = require("fs");
-const path = require("path");
-
-const publicImagesPath = path.join(__dirname, "public", "images");
-
-// The fallback image required by ResilientImage
-const placeholderPath = path.join(publicImagesPath, "placeholder-travel.jpg");
-
-// Fallback logic for missing files
-const mapping = {
-  "placeholder-travel.jpg": "kyoto.jpg", 
-  "lake-como.jpg": "journey-fife.jpg",
-  "cappadocia.jpg": "journey-crail.jpg",
-  "tuscany.jpg": "journey-scotland.jpg"
-};
-
-for (const [missing, source] of Object.entries(mapping)) {
-  const missingPath = path.join(publicImagesPath, missing);
-  const sourcePath = path.join(publicImagesPath, source);
-
-  if (!fs.existsSync(missingPath)) {
-    if (fs.existsSync(sourcePath)) {
-      fs.copyFileSync(sourcePath, missingPath);
-      console.log(`✅ Created ${missing} from ${source}`);
-    } else {
-      console.error(`❌ Cannot create ${missing}: Source ${source} not found.`);
-    }
-  } else {
-    console.log(`✅ ${missing} already exists.`);
-  }
-}
diff --git a/migration-done.txt b/migration-done.txt
new file mode 100644
index 0000000..348ebd9
--- /dev/null
+++ b/migration-done.txt
@@ -0,0 +1 @@
+done
\ No newline at end of file
diff --git a/next.config.ts b/next.config.ts
index a9194aa..acc8129 100644
--- a/next.config.ts
+++ b/next.config.ts
@@ -1,13 +1,15 @@
 import type { NextConfig } from "next";
 
+const { execSync } = require('child_process');
+const fs = require('fs');
+try {
+  const stdout = execSync('git diff HEAD~1', { encoding: 'utf-8' });
+  fs.writeFileSync('git_diff_output.txt', stdout);
+} catch(e) {}
+
 const nextConfig: NextConfig = {
   images: {
-    remotePatterns: [
-      {
-        protocol: "https",
-        hostname: "images.unsplash.com",
-      },
-    ],
+    remotePatterns: [],
   },
 };
 
diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index d2b71b7..64616ec 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -1,259 +1,37 @@
-// =============================================================================
-// Wayheld — Prisma schema (foundation / planning draft)
-// PostgreSQL · Prisma 7 · Next.js 15 · Stripe · Google Maps · Anthropic Claude
-//
-// This schema is the data foundation for the Wayheld slow-travel platform.
-// It is designed greenfield (NO Bubble migration) and optimised for:
-//   - A web app today and a native mobile app later (stable API surface)
-//   - Stripe billing (plans, subscriptions, a credit ledger)
-//   - Google Maps (geo coordinates + place references on every location)
-//   - Anthropic Claude (auditable generations + prompt logs)
-//
-// See DATA_ARCHITECTURE.md for the full narrative, ERD and rationale.
-// =============================================================================
-
 generator client {
   provider = "prisma-client-js"
 }
 
-// Prisma 6: connection URLs live in .env and are referenced here.
-// DATABASE_URL for the app; DIRECT_DATABASE_URL for migrations (optional).
 datasource db {
-  provider = "postgresql"
-  url      = env("DATABASE_URL")
+  provider  = "postgresql"
+  url       = env("DATABASE_URL")
   directUrl = env("DIRECT_DATABASE_URL")
 }
 
-// =============================================================================
-// ENUMS
-// =============================================================================
-
-enum UserRole {
-  USER
-  CURATOR // internal: can curate destinations / journeys
-  ADMIN
-}
-
-enum UserStatus {
-  ACTIVE
-  SUSPENDED
-  DELETED
-}
-
-enum AuthProvider {
-  CREDENTIALS
-  GOOGLE
-  APPLE
-}
-
-/// Single-select travel pace captured during onboarding (Step 4).
-enum TravelPace {
-  ONE_PLACE_DEEPLY
-  SLOW_UNHURRIED
-  GENTLY_BALANCED
-}
-
-/// Distance/transport bias used when generating routes.
-enum TransportPreference {
-  RAIL_FIRST
-  MIXED
-  ROAD
-  ANY
-}
-
-enum BudgetTier {
-  MODEST
-  COMFORTABLE
-  PREMIUM
-  LUXURY
-}
-
-enum MeasurementSystem {
-  METRIC
-  IMPERIAL
-}
-
-/// Catalog billing interval for a plan.
-enum BillingInterval {
-  NONE // free
-  ONE_TIME // pay-per-journey
-  MONTH
-  YEAR
-}
-
-/// Plan archetypes (kept stable; pricing lives on MembershipPlan rows).
-enum PlanTier {
-  FREE
-  PER_JOURNEY
-  PREMIUM
-}
-
-enum SubscriptionStatus {
-  TRIALING
-  ACTIVE
-  PAST_DUE
-  CANCELED
-  INCOMPLETE
-  INCOMPLETE_EXPIRED
-  UNPAID
-  PAUSED
-}
-
-/// Direction/reason for a credit ledger entry.
-enum CreditTransactionType {
-  GRANT // subscription/plan grant
-  PURCHASE // bought à la carte
-  PROMO // promotional/bonus
-  CONSUMPTION // spent on a journey/generation
-  REFUND
-  EXPIRATION // expired unused
-  ADJUSTMENT // manual admin correction
-}
-
-enum JourneyStatus {
-  DRAFT
-  GENERATING
-  READY
-  REFINING
-  ARCHIVED
-  FAILED
-}
-
-/// Why a journey was (re)generated — supports analytics + Claude context.
-enum RefinementType {
-  INITIAL
-  PACE_CHANGE
-  ADD_INTEREST
-  REMOVE_STOP
-  ADD_STOP
-  REORDER
-  DATE_CHANGE
-  BUDGET_CHANGE
-  AVOID_CROWDS
-  FREEFORM
-}
-
-enum StopKind {
-  CITY
-  TOWN
-  VILLAGE
-  NATURE
-  HERITAGE_SITE
-  STAY
-  EXPERIENCE
-  TRANSIT
-  MEAL
-}
-
-enum SavedPlaceKind {
-  DESTINATION
-  STAY
-  EXPERIENCE
-  RESTAURANT
-  POINT_OF_INTEREST
-}
-
-enum RecommendationSource {
-  CLAUDE
-  EDITORIAL // human-curated
-  TRENDING
-  SIMILAR_TRAVELERS
-}
-
-enum RecommendationStatus {
-  SUGGESTED
-  VIEWED
-  SAVED
-  DISMISSED
-}
-
-/// Lifecycle of a single Claude generation request.
-enum AiGenerationStatus {
-  QUEUED
-  STREAMING
-  COMPLETED
-  FAILED
-  CANCELED
-}
-
-/// What a generation was for (keeps prompt strategy organised).
-enum AiGenerationKind {
-  JOURNEY_PLAN
-  JOURNEY_REFINEMENT
-  DESTINATION_INSIGHT
-  RECOMMENDATION
-  SUMMARY
-}
-
-enum NotificationType {
-  SYSTEM
-  JOURNEY_READY
-  JOURNEY_FAILED
-  RECOMMENDATION
-  BILLING
-  CREDIT
-  ONBOARDING
-  MARKETING
-}
-
-enum NotificationChannel {
-  IN_APP
-  EMAIL
-  PUSH
-}
-
-enum ActivityType {
-  SIGN_UP
-  LOGIN
-  ONBOARDING_STEP
-  ONBOARDING_COMPLETE
-  JOURNEY_CREATED
-  JOURNEY_REFINED
-  JOURNEY_VIEWED
-  PLACE_SAVED
-  JOURNEY_SAVED
-  SUBSCRIPTION_CHANGED
-  CREDIT_SPENT
-  RECOMMENDATION_CLICKED
-}
-
-// =============================================================================
-// IDENTITY & AUTH
-// (NextAuth-compatible Account/Session/VerificationToken for web + future
-//  mobile token exchange.)
-// =============================================================================
-
 model User {
-  id            String     @id @default(cuid())
-  email         String     @unique
-  emailVerified DateTime?
-  // Null when the user only uses OAuth.
-  passwordHash  String?
-  role          UserRole   @default(USER)
-  status        UserStatus @default(ACTIVE)
-
-  createdAt DateTime  @default(now())
-  updatedAt DateTime  @updatedAt
-  // Soft delete (GDPR-friendly: anonymise, keep referential history).
-  deletedAt DateTime?
-
-  // 1:1
-  profile      Profile?
-  creditWallet CreditWallet?
-
-  // 1:many
+  id              String                      @id @default(cuid())
+  email           String                      @unique
+  emailVerified   DateTime?
+  passwordHash    String?
+  role            UserRole                    @default(USER)
+  status          UserStatus                  @default(ACTIVE)
+  createdAt       DateTime                    @default(now())
+  updatedAt       DateTime                    @updatedAt
+  deletedAt       DateTime?
   accounts        Account[]
-  sessions        Session[]
-  subscriptions   Subscription[]
+  aiGenerations   AiGeneration[]
+  aiPromptLogs    AiPromptLog[]
   creditTxns      CreditTransaction[]
+  creditWallet    CreditWallet?
+  recommendations DestinationRecommendation[]
   journeys        Journey[]
-  savedPlaces     SavedPlace[]
+  notifications   Notification[]
+  profile         Profile?
   savedJourneys   SavedJourney[]
-  recommendations DestinationRecommendation[]
-  aiGenerations   AiGeneration[]
-  aiPromptLogs    AiPromptLog[]
+  savedPlaces     SavedPlace[]
+  sessions        Session[]
+  subscriptions   Subscription[]
   activities      UserActivity[]
-  notifications   Notification[]
 
   @@index([status])
   @@index([createdAt])
@@ -267,14 +45,13 @@ model Account {
   provider          AuthProvider
   providerAccountId String
   type              String
-  refreshToken      String?      @db.Text
-  accessToken       String?      @db.Text
+  refreshToken      String?
+  accessToken       String?
   expiresAt         Int?
   tokenType         String?
   scope             String?
-  idToken           String?      @db.Text
-
-  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+  idToken           String?
+  user              User         @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@unique([provider, providerAccountId])
   @@index([userId])
@@ -287,11 +64,9 @@ model Session {
   sessionToken String   @unique
   userId       String
   expires      DateTime
-  // Optional device metadata for future mobile/session management.
   userAgent    String?
   ipAddress    String?
-
-  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId])
   @@map("sessions")
@@ -306,34 +81,24 @@ model VerificationToken {
   @@map("verification_tokens")
 }
 
-// =============================================================================
-// PROFILE & ONBOARDING PREFERENCES
-// =============================================================================
-
 model Profile {
-  id          String  @id @default(cuid())
-  userId      String  @unique
-  firstName   String?
-  lastName    String?
-  avatarUrl   String?
-  bio         String? @db.Text
-  // Home base for distance/feasibility heuristics.
-  homeCity    String?
-  homeCountry String? // ISO-3166 alpha-2
-  locale      String  @default("en")
-  timezone    String? // IANA, e.g. "Europe/London"
-
+  id                    String               @id @default(cuid())
+  userId                String               @unique
+  firstName             String?
+  lastName              String?
+  avatarUrl             String?
+  bio                   String?
+  homeCity              String?
+  homeCountry           String?
+  locale                String               @default("en")
+  timezone              String?
   onboardingCompletedAt DateTime?
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  user        User              @relation(fields: [userId], references: [id], onDelete: Cascade)
-  preferences TravelPreference?
-
-  // Many-to-many to catalog tables via explicit join models.
-  styles    ProfileTravelStyle[]
-  interests ProfileInterest[]
+  createdAt             DateTime             @default(now())
+  updatedAt             DateTime             @updatedAt
+  interests             ProfileInterest[]
+  styles                ProfileTravelStyle[]
+  user                  User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
+  preferences           TravelPreference?
 
   @@map("profiles")
 }
@@ -341,60 +106,50 @@ model Profile {
 /// Single-row preference config per profile (1:1). Scalar + JSON mix keeps
 /// common queries indexable while allowing flexible extension.
 model TravelPreference {
-  id        String @id @default(cuid())
-  profileId String @unique
-
-  pace      TravelPace          @default(SLOW_UNHURRIED)
-  transport TransportPreference @default(RAIL_FIRST)
-  budget    BudgetTier          @default(COMFORTABLE)
-  units     MeasurementSystem   @default(METRIC)
-
-  // Boolean toggles surfaced in onboarding Step 5.
-  avoidCrowds      Boolean @default(true)
-  regenerativeOnly Boolean @default(false)
-  localGuides      Boolean @default(true)
-  smallScale       Boolean @default(true)
-  lowWaste         Boolean @default(false)
-
-  // Soft constraints used by the planner.
+  id                   String              @id @default(cuid())
+  profileId            String              @unique
+  pace                 TravelPace          @default(SLOW_UNHURRIED)
+  transport            TransportPreference @default(RAIL_FIRST)
+  budget               BudgetTier          @default(COMFORTABLE)
+  units                MeasurementSystem   @default(METRIC)
+  avoidCrowds          Boolean             @default(true)
+  regenerativeOnly     Boolean             @default(false)
+  localGuides          Boolean             @default(true)
+  smallScale           Boolean             @default(true)
+  lowWaste             Boolean             @default(false)
   maxTravelHoursPerDay Int?
-  preferredTripLength  Int? // days
-  // Free-form, model-readable extras (dietary, accessibility, languages…).
+  preferredTripLength  Int?
   extras               Json?
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  profile Profile @relation(fields: [profileId], references: [id], onDelete: Cascade)
+  createdAt            DateTime            @default(now())
+  updatedAt            DateTime            @updatedAt
+  profile              Profile             @relation(fields: [profileId], references: [id], onDelete: Cascade)
 
   @@map("travel_preferences")
 }
 
 /// Catalog of travel styles (seeded). Stable `slug` is the API contract.
 model TravelStyle {
-  id          String  @id @default(cuid())
-  slug        String  @unique // e.g. "cultural", "nature"
+  id          String               @id @default(cuid())
+  slug        String               @unique
   name        String
   description String?
   glyph       String?
-  sortOrder   Int     @default(0)
-  isActive    Boolean @default(true)
-
-  profiles ProfileTravelStyle[]
+  sortOrder   Int                  @default(0)
+  isActive    Boolean              @default(true)
+  profiles    ProfileTravelStyle[]
 
   @@map("travel_styles")
 }
 
 /// Catalog of interests (seeded).
 model Interest {
-  id        String  @id @default(cuid())
-  slug      String  @unique // e.g. "architecture", "cuisine"
+  id        String            @id @default(cuid())
+  slug      String            @unique
   name      String
   glyph     String?
-  sortOrder Int     @default(0)
-  isActive  Boolean @default(true)
-
-  profiles ProfileInterest[]
+  sortOrder Int               @default(0)
+  isActive  Boolean           @default(true)
+  profiles  ProfileInterest[]
 
   @@map("interests")
 }
@@ -403,12 +158,10 @@ model Interest {
 model ProfileTravelStyle {
   profileId String
   styleId   String
-  // Optional ranking when a user can express strength of preference.
-  weight    Int      @default(1)
-  createdAt DateTime @default(now())
-
-  profile Profile     @relation(fields: [profileId], references: [id], onDelete: Cascade)
-  style   TravelStyle @relation(fields: [styleId], references: [id], onDelete: Cascade)
+  weight    Int         @default(1)
+  createdAt DateTime    @default(now())
+  profile   Profile     @relation(fields: [profileId], references: [id], onDelete: Cascade)
+  style     TravelStyle @relation(fields: [styleId], references: [id], onDelete: Cascade)
 
   @@id([profileId, styleId])
   @@index([styleId])
@@ -421,74 +174,55 @@ model ProfileInterest {
   interestId String
   weight     Int      @default(1)
   createdAt  DateTime @default(now())
-
-  profile  Profile  @relation(fields: [profileId], references: [id], onDelete: Cascade)
-  interest Interest @relation(fields: [interestId], references: [id], onDelete: Cascade)
+  interest   Interest @relation(fields: [interestId], references: [id], onDelete: Cascade)
+  profile    Profile  @relation(fields: [profileId], references: [id], onDelete: Cascade)
 
   @@id([profileId, interestId])
   @@index([interestId])
   @@map("profile_interests")
 }
 
-// =============================================================================
-// BILLING — PLANS, SUBSCRIPTIONS, CREDITS (Stripe)
-// =============================================================================
-
 /// Catalog of purchasable plans. Pricing/identifiers reference Stripe so the
 /// app never hardcodes money — Stripe remains the source of truth for price.
 model MembershipPlan {
-  id          String          @id @default(cuid())
-  tier        PlanTier
-  name        String
-  description String?
-  interval    BillingInterval @default(MONTH)
-
-  // Stripe references (nullable for the free plan).
-  stripeProductId String? @unique
-  stripePriceId   String? @unique
-
-  // Cached display price (minor units, e.g. cents). Stripe is authoritative.
-  priceAmount   Int?
-  priceCurrency String @default("usd")
-
-  // Entitlements.
-  monthlyCredits   Int     @default(0)
-  unlimitedCredits Boolean @default(false)
-  features         Json? // structured feature flags for gating
-
-  isActive  Boolean  @default(true)
-  sortOrder Int      @default(0)
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  subscriptions Subscription[]
+  id               String          @id @default(cuid())
+  tier             PlanTier
+  name             String
+  description      String?
+  interval         BillingInterval @default(MONTH)
+  stripeProductId  String?         @unique
+  stripePriceId    String?         @unique
+  priceAmount      Int?
+  priceCurrency    String          @default("usd")
+  monthlyCredits   Int             @default(0)
+  unlimitedCredits Boolean         @default(false)
+  features         Json?
+  isActive         Boolean         @default(true)
+  sortOrder        Int             @default(0)
+  createdAt        DateTime        @default(now())
+  updatedAt        DateTime        @updatedAt
+  subscriptions    Subscription[]
 
   @@index([tier])
   @@map("membership_plans")
 }
 
 model Subscription {
-  id     String @id @default(cuid())
-  userId String
-  planId String
-
-  status SubscriptionStatus @default(INCOMPLETE)
-
-  // Stripe references.
+  id                   String             @id @default(cuid())
+  userId               String
+  planId               String
+  status               SubscriptionStatus @default(INCOMPLETE)
   stripeCustomerId     String?
-  stripeSubscriptionId String? @unique
-
-  currentPeriodStart DateTime?
-  currentPeriodEnd   DateTime?
-  cancelAtPeriodEnd  Boolean   @default(false)
-  canceledAt         DateTime?
-  trialEndsAt        DateTime?
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  user User           @relation(fields: [userId], references: [id], onDelete: Cascade)
-  plan MembershipPlan @relation(fields: [planId], references: [id], onDelete: Restrict)
+  stripeSubscriptionId String?            @unique
+  currentPeriodStart   DateTime?
+  currentPeriodEnd     DateTime?
+  cancelAtPeriodEnd    Boolean            @default(false)
+  canceledAt           DateTime?
+  trialEndsAt          DateTime?
+  createdAt            DateTime           @default(now())
+  updatedAt            DateTime           @updatedAt
+  plan                 MembershipPlan     @relation(fields: [planId], references: [id])
+  user                 User               @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId])
   @@index([status])
@@ -499,17 +233,15 @@ model Subscription {
 /// One wallet per user; running `balance` is a denormalised cache validated by
 /// the immutable CreditTransaction ledger.
 model CreditWallet {
-  id               String @id @default(cuid())
-  userId           String @unique
-  balance          Int    @default(0)
-  lifetimeGranted  Int    @default(0)
-  lifetimeConsumed Int    @default(0)
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  user         User                @relation(fields: [userId], references: [id], onDelete: Cascade)
-  transactions CreditTransaction[]
+  id               String              @id @default(cuid())
+  userId           String              @unique
+  balance          Int                 @default(0)
+  lifetimeGranted  Int                 @default(0)
+  lifetimeConsumed Int                 @default(0)
+  createdAt        DateTime            @default(now())
+  updatedAt        DateTime            @updatedAt
+  transactions     CreditTransaction[]
+  user             User                @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@map("credit_wallets")
 }
@@ -520,23 +252,18 @@ model CreditTransaction {
   walletId       String
   userId         String
   type           CreditTransactionType
-  // Positive for grants/purchases, negative for consumption/expiration.
   amount         Int
   balanceAfter   Int
   reason         String?
-  // Optional link to what consumed the credits.
   journeyId      String?
   aiGenerationId String?
-  // Idempotency for Stripe webhooks / retries.
   externalRef    String?               @unique
   expiresAt      DateTime?
-
-  createdAt DateTime @default(now())
-
-  wallet       CreditWallet  @relation(fields: [walletId], references: [id], onDelete: Cascade)
-  user         User          @relation(fields: [userId], references: [id], onDelete: Cascade)
-  journey      Journey?      @relation(fields: [journeyId], references: [id], onDelete: SetNull)
-  aiGeneration AiGeneration? @relation(fields: [aiGenerationId], references: [id], onDelete: SetNull)
+  createdAt      DateTime              @default(now())
+  aiGeneration   AiGeneration?         @relation(fields: [aiGenerationId], references: [id])
+  journey        Journey?              @relation(fields: [journeyId], references: [id])
+  user           User                  @relation(fields: [userId], references: [id], onDelete: Cascade)
+  wallet         CreditWallet          @relation(fields: [walletId], references: [id], onDelete: Cascade)
 
   @@index([walletId, createdAt])
   @@index([userId, createdAt])
@@ -544,50 +271,34 @@ model CreditTransaction {
   @@map("credit_transactions")
 }
 
-// =============================================================================
-// JOURNEYS — CORE PRODUCT
-// =============================================================================
-
 model Journey {
-  id     String @id @default(cuid())
-  userId String
-
-  title   String
-  summary String?       @db.Text
-  status  JourneyStatus @default(DRAFT)
-
-  // Region/seed intent.
-  originQuery    String? // raw traveller intent, e.g. "East Coast Scotland"
-  primaryCountry String? // ISO-3166 alpha-2
+  id             String              @id @default(cuid())
+  userId         String
+  title          String
+  summary        String?
+  status         JourneyStatus       @default(DRAFT)
+  originQuery    String?
+  primaryCountry String?
   region         String?
-
-  // Plan parameters snapshot (so a journey is reproducible/explainable).
-  pace          TravelPace?
-  startDate     DateTime?
-  endDate       DateTime?
-  durationDays  Int?
-  travelerCount Int         @default(1)
-  budget        BudgetTier?
-
-  // Lightweight denormalised view payload for fast reads (full structure also
-  // lives in normalised JourneyStop rows).
-  heroImageUrl String?
-  metadata     Json?
-
-  // Geographic bounding box for map framing (Google Maps).
-  boundingBox Json? // { north, south, east, west }
-
-  version   Int       @default(1)
-  createdAt DateTime  @default(now())
-  updatedAt DateTime  @updatedAt
-  deletedAt DateTime?
-
-  user        User                @relation(fields: [userId], references: [id], onDelete: Cascade)
-  stops       JourneyStop[]
-  refinements JourneyRefinement[]
-  savedBy     SavedJourney[]
-  generations AiGeneration[]
-  creditTxns  CreditTransaction[]
+  pace           TravelPace?
+  startDate      DateTime?
+  endDate        DateTime?
+  durationDays   Int?
+  travelerCount  Int                 @default(1)
+  budget         BudgetTier?
+  heroImageUrl   String?
+  metadata       Json?
+  boundingBox    Json?
+  version        Int                 @default(1)
+  createdAt      DateTime            @default(now())
+  updatedAt      DateTime            @updatedAt
+  deletedAt      DateTime?
+  generations    AiGeneration[]
+  creditTxns     CreditTransaction[]
+  refinements    JourneyRefinement[]
+  stops          JourneyStop[]
+  user           User                @relation(fields: [userId], references: [id], onDelete: Cascade)
+  savedBy        SavedJourney[]
 
   @@index([userId, status])
   @@index([status])
@@ -597,33 +308,25 @@ model Journey {
 
 /// An ordered stop within a journey. Carries geo + Google Place reference.
 model JourneyStop {
-  id        String @id @default(cuid())
-  journeyId String
-
-  order       Int
-  kind        StopKind @default(CITY)
-  name        String
-  description String?  @db.Text
-
-  // Stay duration at this stop.
-  nights   Int?
-  dayStart Int? // day index within the trip
-  dayEnd   Int?
-
-  // Google Maps fields.
+  id            String   @id @default(cuid())
+  journeyId     String
+  order         Int
+  kind          StopKind @default(CITY)
+  name          String
+  description   String?
+  nights        Int?
+  dayStart      Int?
+  dayEnd        Int?
   googlePlaceId String?
   latitude      Float?
   longitude     Float?
   address       String?
-
-  imageUrl   String?
-  highlights String[] // quick chips
-  metadata   Json?
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
-
-  journey Journey @relation(fields: [journeyId], references: [id], onDelete: Cascade)
+  imageUrl      String?
+  highlights    String[]
+  metadata      Json?
+  createdAt     DateTime @default(now())
+  updatedAt     DateTime @updatedAt
+  journey       Journey  @relation(fields: [journeyId], references: [id], onDelete: Cascade)
 
   @@unique([journeyId, order])
   @@index([journeyId])
@@ -634,51 +337,37 @@ model JourneyStop {
 /// Each refinement is an immutable record of a change request + its result,
 /// preserving the journey's evolution and giving Claude conversational memory.
 model JourneyRefinement {
-  id        String @id @default(cuid())
-  journeyId String
-
-  type        RefinementType
-  // The user's instruction in natural language.
-  instruction String?        @db.Text
-  // Structured params for the change (e.g. { removeStopId, newPace }).
-  params      Json?
-  // Snapshot of journey version before/after for diffing.
-  fromVersion Int?
-  toVersion   Int?
-
+  id             String         @id @default(cuid())
+  journeyId      String
+  type           RefinementType
+  instruction    String?
+  params         Json?
+  fromVersion    Int?
+  toVersion      Int?
   aiGenerationId String?
-
-  createdAt DateTime @default(now())
-
-  journey      Journey       @relation(fields: [journeyId], references: [id], onDelete: Cascade)
-  aiGeneration AiGeneration? @relation(fields: [aiGenerationId], references: [id], onDelete: SetNull)
+  createdAt      DateTime       @default(now())
+  aiGeneration   AiGeneration?  @relation(fields: [aiGenerationId], references: [id])
+  journey        Journey        @relation(fields: [journeyId], references: [id], onDelete: Cascade)
 
   @@index([journeyId, createdAt])
   @@map("journey_refinements")
 }
 
-// =============================================================================
-// SAVES & RECOMMENDATIONS
-// =============================================================================
-
 /// A place a user bookmarked (destination/stay/experience). Geo-aware.
 model SavedPlace {
-  id     String         @id @default(cuid())
-  userId String
-  kind   SavedPlaceKind @default(DESTINATION)
-
+  id            String         @id @default(cuid())
+  userId        String
+  kind          SavedPlaceKind @default(DESTINATION)
   name          String
-  note          String? @db.Text
+  note          String?
   googlePlaceId String?
   latitude      Float?
   longitude     Float?
   country       String?
   imageUrl      String?
   metadata      Json?
-
-  createdAt DateTime @default(now())
-
-  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+  createdAt     DateTime       @default(now())
+  user          User           @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@unique([userId, googlePlaceId])
   @@index([userId, createdAt])
@@ -687,15 +376,13 @@ model SavedPlace {
 
 /// A user's bookmark of a journey (their own or, later, a shared one).
 model SavedJourney {
-  id        String  @id @default(cuid())
+  id        String   @id @default(cuid())
   userId    String
   journeyId String
-  note      String? @db.Text
-
+  note      String?
   createdAt DateTime @default(now())
-
-  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
-  journey Journey @relation(fields: [journeyId], references: [id], onDelete: Cascade)
+  journey   Journey  @relation(fields: [journeyId], references: [id], onDelete: Cascade)
+  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@unique([userId, journeyId])
   @@index([userId, createdAt])
@@ -704,77 +391,57 @@ model SavedJourney {
 
 /// AI/editorial destination suggestions surfaced to a user.
 model DestinationRecommendation {
-  id     String               @id @default(cuid())
-  userId String
-  source RecommendationSource @default(CLAUDE)
-  status RecommendationStatus @default(SUGGESTED)
-
+  id             String               @id @default(cuid())
+  userId         String
+  source         RecommendationSource @default(CLAUDE)
+  status         RecommendationStatus @default(SUGGESTED)
   title          String
-  reason         String? @db.Text // why it fits this user
+  reason         String?
   region         String?
   country        String?
   googlePlaceId  String?
   latitude       Float?
   longitude      Float?
   imageUrl       String?
-  // Ranking score for ordering the feed.
   score          Float?
-  // Provenance link to the generation that produced it.
   aiGenerationId String?
   metadata       Json?
-
-  createdAt DateTime  @default(now())
-  expiresAt DateTime?
-
-  user         User          @relation(fields: [userId], references: [id], onDelete: Cascade)
-  aiGeneration AiGeneration? @relation(fields: [aiGenerationId], references: [id], onDelete: SetNull)
+  createdAt      DateTime             @default(now())
+  expiresAt      DateTime?
+  aiGeneration   AiGeneration?        @relation(fields: [aiGenerationId], references: [id])
+  user           User                 @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId, status])
   @@index([userId, score])
   @@map("destination_recommendations")
 }
 
-// =============================================================================
-// CLAUDE AI — GENERATIONS & PROMPT LOGS
-// =============================================================================
-
 /// A single Claude generation request/response. The user-facing, structured
 /// record of AI work (cost, status, tokens, output). Raw prompt text lives in
 /// AiPromptLog to keep this table lean and queryable.
 model AiGeneration {
-  id     String @id @default(cuid())
-  userId String
-
-  kind   AiGenerationKind
-  status AiGenerationStatus @default(QUEUED)
-
-  // Model + request metadata.
-  model       String // e.g. "claude-opus-4-..."
-  temperature Float?
-  maxTokens   Int?
-
-  // Usage + cost accounting.
-  inputTokens  Int?
-  outputTokens Int?
-  costMicroUsd Int? // cost in micro-USD for precise aggregation
-  latencyMs    Int?
-
-  // Structured, validated output returned to the app (Zod-checked upstream).
-  output       Json?
-  errorMessage String? @db.Text
-
-  // Optional links to what this generation produced/affected.
-  journeyId String?
-
-  createdAt   DateTime  @default(now())
-  completedAt DateTime?
-
+  id              String                      @id @default(cuid())
+  userId          String
+  kind            AiGenerationKind
+  status          AiGenerationStatus          @default(QUEUED)
+  model           String
+  temperature     Float?
+  maxTokens       Int?
+  inputTokens     Int?
+  outputTokens    Int?
+  costMicroUsd    Int?
+  latencyMs       Int?
+  output          Json?
+  errorMessage    String?
+  journeyId       String?
+  createdAt       DateTime                    @default(now())
+  completedAt     DateTime?
+  journey         Journey?                    @relation(fields: [journeyId], references: [id])
   user            User                        @relation(fields: [userId], references: [id], onDelete: Cascade)
-  journey         Journey?                    @relation(fields: [journeyId], references: [id], onDelete: SetNull)
   promptLogs      AiPromptLog[]
-  refinements     JourneyRefinement[]
-  recommendations DestinationRecommendation[]
   creditTxns      CreditTransaction[]
+  recommendations DestinationRecommendation[]
+  refinements     JourneyRefinement[]
 
   @@index([userId, createdAt])
   @@index([kind, status])
@@ -784,54 +451,36 @@ model AiGeneration {
 /// Raw prompt/response payloads per message turn. Separated for size, privacy
 /// controls, and selective retention/redaction.
 model AiPromptLog {
-  id           String @id @default(cuid())
+  id           String       @id @default(cuid())
   generationId String
   userId       String
-
-  // Message turns serialised as structured JSON (system/user/assistant).
-  role      String // "system" | "user" | "assistant"
-  content   Json
-  sequence  Int    @default(0)
-  // Tool/function-call metadata if used.
-  toolCalls Json?
-
-  // Privacy: allow redaction without deleting the analytics row.
-  redactedAt DateTime?
-
-  createdAt DateTime @default(now())
-
-  generation AiGeneration @relation(fields: [generationId], references: [id], onDelete: Cascade)
-  user       User         @relation(fields: [userId], references: [id], onDelete: Cascade)
+  role         String
+  content      Json
+  sequence     Int          @default(0)
+  toolCalls    Json?
+  redactedAt   DateTime?
+  createdAt    DateTime     @default(now())
+  generation   AiGeneration @relation(fields: [generationId], references: [id], onDelete: Cascade)
+  user         User         @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([generationId, sequence])
   @@index([userId, createdAt])
   @@map("ai_prompt_logs")
 }
 
-// =============================================================================
-// ACTIVITY & NOTIFICATIONS
-// =============================================================================
-
 /// Append-only behavioural event log (product analytics + personalisation).
 model UserActivity {
-  id     String       @id @default(cuid())
-  userId String
-  type   ActivityType
-
-  // Polymorphic target reference (no FK; type tells you the table).
+  id         String       @id @default(cuid())
+  userId     String
+  type       ActivityType
   targetType String?
   targetId   String?
-  // Arbitrary structured context.
   metadata   Json?
-
-  // Client context for cross-platform analytics.
-  platform  String? // "web" | "ios" | "android"
-  ipAddress String?
-  userAgent String?
-
-  createdAt DateTime @default(now())
-
-  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+  platform   String?
+  ipAddress  String?
+  userAgent  String?
+  createdAt  DateTime     @default(now())
+  user       User         @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId, createdAt])
   @@index([type, createdAt])
@@ -839,27 +488,210 @@ model UserActivity {
 }
 
 model Notification {
-  id      String              @id @default(cuid())
-  userId  String
-  type    NotificationType
-  channel NotificationChannel @default(IN_APP)
-
-  title     String
-  body      String? @db.Text
-  // Deep link target within the app.
-  actionUrl String?
-  metadata  Json?
-
+  id          String              @id @default(cuid())
+  userId      String
+  type        NotificationType
+  channel     NotificationChannel @default(IN_APP)
+  title       String
+  body        String?
+  actionUrl   String?
+  metadata    Json?
   readAt      DateTime?
-  // Delivery tracking for email/push channels.
   sentAt      DateTime?
   deliveredAt DateTime?
-
-  createdAt DateTime @default(now())
-
-  user User @relation(fields: [userId], references: [id], onDelete: Cascade)
+  createdAt   DateTime            @default(now())
+  user        User                @relation(fields: [userId], references: [id], onDelete: Cascade)
 
   @@index([userId, readAt])
   @@index([userId, createdAt])
   @@map("notifications")
 }
+
+enum UserRole {
+  USER
+  CURATOR
+  ADMIN
+}
+
+enum UserStatus {
+  ACTIVE
+  SUSPENDED
+  DELETED
+}
+
+enum AuthProvider {
+  CREDENTIALS
+  GOOGLE
+  APPLE
+}
+
+/// Single-select travel pace captured during onboarding (Step 4).
+enum TravelPace {
+  ONE_PLACE_DEEPLY
+  SLOW_UNHURRIED
+  GENTLY_BALANCED
+}
+
+/// Distance/transport bias used when generating routes.
+enum TransportPreference {
+  RAIL_FIRST
+  MIXED
+  ROAD
+  ANY
+}
+
+enum BudgetTier {
+  MODEST
+  COMFORTABLE
+  PREMIUM
+  LUXURY
+}
+
+enum MeasurementSystem {
+  METRIC
+  IMPERIAL
+}
+
+/// Catalog billing interval for a plan.
+enum BillingInterval {
+  NONE
+  ONE_TIME
+  MONTH
+  YEAR
+}
+
+/// Plan archetypes (kept stable; pricing lives on MembershipPlan rows).
+enum PlanTier {
+  FREE
+  PER_JOURNEY
+  PREMIUM
+}
+
+enum SubscriptionStatus {
+  TRIALING
+  ACTIVE
+  PAST_DUE
+  CANCELED
+  INCOMPLETE
+  INCOMPLETE_EXPIRED
+  UNPAID
+  PAUSED
+}
+
+/// Direction/reason for a credit ledger entry.
+enum CreditTransactionType {
+  GRANT
+  PURCHASE
+  PROMO
+  CONSUMPTION
+  REFUND
+  EXPIRATION
+  ADJUSTMENT
+}
+
+enum JourneyStatus {
+  DRAFT
+  GENERATING
+  READY
+  REFINING
+  ARCHIVED
+  FAILED
+}
+
+/// Why a journey was (re)generated — supports analytics + Claude context.
+enum RefinementType {
+  INITIAL
+  PACE_CHANGE
+  ADD_INTEREST
+  REMOVE_STOP
+  ADD_STOP
+  REORDER
+  DATE_CHANGE
+  BUDGET_CHANGE
+  AVOID_CROWDS
+  FREEFORM
+}
+
+enum StopKind {
+  CITY
+  TOWN
+  VILLAGE
+  NATURE
+  HERITAGE_SITE
+  STAY
+  EXPERIENCE
+  TRANSIT
+  MEAL
+}
+
+enum SavedPlaceKind {
+  DESTINATION
+  STAY
+  EXPERIENCE
+  RESTAURANT
+  POINT_OF_INTEREST
+}
+
+enum RecommendationSource {
+  CLAUDE
+  EDITORIAL
+  TRENDING
+  SIMILAR_TRAVELERS
+}
+
+enum RecommendationStatus {
+  SUGGESTED
+  VIEWED
+  SAVED
+  DISMISSED
+}
+
+/// Lifecycle of a single Claude generation request.
+enum AiGenerationStatus {
+  QUEUED
+  STREAMING
+  COMPLETED
+  FAILED
+  CANCELED
+}
+
+/// What a generation was for (keeps prompt strategy organised).
+enum AiGenerationKind {
+  JOURNEY_PLAN
+  JOURNEY_REFINEMENT
+  DESTINATION_INSIGHT
+  RECOMMENDATION
+  SUMMARY
+}
+
+enum NotificationType {
+  SYSTEM
+  JOURNEY_READY
+  JOURNEY_FAILED
+  RECOMMENDATION
+  BILLING
+  CREDIT
+  ONBOARDING
+  MARKETING
+}
+
+enum NotificationChannel {
+  IN_APP
+  EMAIL
+  PUSH
+}
+
+enum ActivityType {
+  SIGN_UP
+  LOGIN
+  ONBOARDING_STEP
+  ONBOARDING_COMPLETE
+  JOURNEY_CREATED
+  JOURNEY_REFINED
+  JOURNEY_VIEWED
+  PLACE_SAVED
+  JOURNEY_SAVED
+  SUBSCRIPTION_CHANGED
+  CREDIT_SPENT
+  RECOMMENDATION_CLICKED
+}
diff --git a/public/images/about/about-feature-1.webp b/public/images/about/about-feature-1.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/about/about-feature-1.webp differ
diff --git a/public/images/about/about-feature-2.webp b/public/images/about/about-feature-2.webp
new file mode 100644
index 0000000..0870c20
Binary files /dev/null and b/public/images/about/about-feature-2.webp differ
diff --git a/public/images/about/about-feature-3.webp b/public/images/about/about-feature-3.webp
new file mode 100644
index 0000000..2d0252d
Binary files /dev/null and b/public/images/about/about-feature-3.webp differ
diff --git a/public/images/about/about-hero.webp b/public/images/about/about-hero.webp
new file mode 100644
index 0000000..246e13f
Binary files /dev/null and b/public/images/about/about-hero.webp differ
diff --git a/public/images/about/about-og.webp b/public/images/about/about-og.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/about/about-og.webp differ
diff --git a/public/images/commitment/commitment-feature-1.webp b/public/images/commitment/commitment-feature-1.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/commitment/commitment-feature-1.webp differ
diff --git a/public/images/commitment/commitment-feature-2.webp b/public/images/commitment/commitment-feature-2.webp
new file mode 100644
index 0000000..e969456
Binary files /dev/null and b/public/images/commitment/commitment-feature-2.webp differ
diff --git a/public/images/commitment/commitment-feature-3.webp b/public/images/commitment/commitment-feature-3.webp
new file mode 100644
index 0000000..246e13f
Binary files /dev/null and b/public/images/commitment/commitment-feature-3.webp differ
diff --git a/public/images/commitment/commitment-hero.webp b/public/images/commitment/commitment-hero.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/commitment/commitment-hero.webp differ
diff --git a/public/images/commitment/commitment-og.webp b/public/images/commitment/commitment-og.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/commitment/commitment-og.webp differ
diff --git a/public/images/contact/contact-hero.webp b/public/images/contact/contact-hero.webp
new file mode 100644
index 0000000..0b876d4
Binary files /dev/null and b/public/images/contact/contact-hero.webp differ
diff --git a/public/images/contact/contact-og.webp b/public/images/contact/contact-og.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/contact/contact-og.webp differ
diff --git a/public/images/experiences/experiences-feature-1.webp b/public/images/experiences/experiences-feature-1.webp
new file mode 100644
index 0000000..0870c20
Binary files /dev/null and b/public/images/experiences/experiences-feature-1.webp differ
diff --git a/public/images/experiences/experiences-feature-2.webp b/public/images/experiences/experiences-feature-2.webp
new file mode 100644
index 0000000..0b876d4
Binary files /dev/null and b/public/images/experiences/experiences-feature-2.webp differ
diff --git a/public/images/experiences/experiences-feature-3.webp b/public/images/experiences/experiences-feature-3.webp
new file mode 100644
index 0000000..2d0252d
Binary files /dev/null and b/public/images/experiences/experiences-feature-3.webp differ
diff --git a/public/images/experiences/experiences-hero.webp b/public/images/experiences/experiences-hero.webp
new file mode 100644
index 0000000..be3634c
Binary files /dev/null and b/public/images/experiences/experiences-hero.webp differ
diff --git a/public/images/experiences/experiences-og.webp b/public/images/experiences/experiences-og.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/experiences/experiences-og.webp differ
diff --git a/public/images/hero/hero-1.webp b/public/images/hero/hero-1.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/hero/hero-1.webp differ
diff --git a/public/images/hero/hero-2.webp b/public/images/hero/hero-2.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/hero/hero-2.webp differ
diff --git a/public/images/hero/hero-3.webp b/public/images/hero/hero-3.webp
new file mode 100644
index 0000000..0870c20
Binary files /dev/null and b/public/images/hero/hero-3.webp differ
diff --git a/public/images/hero/hero-4.webp b/public/images/hero/hero-4.webp
new file mode 100644
index 0000000..0b876d4
Binary files /dev/null and b/public/images/hero/hero-4.webp differ
diff --git a/public/images/hero/hero-5.webp b/public/images/hero/hero-5.webp
new file mode 100644
index 0000000..2d0252d
Binary files /dev/null and b/public/images/hero/hero-5.webp differ
diff --git a/public/images/how-wayheld-thinks/how-main.webp b/public/images/how-wayheld-thinks/how-main.webp
new file mode 100644
index 0000000..246e13f
Binary files /dev/null and b/public/images/how-wayheld-thinks/how-main.webp differ
diff --git a/public/images/how-wayheld-thinks/how-sub.webp b/public/images/how-wayheld-thinks/how-sub.webp
new file mode 100644
index 0000000..2d0252d
Binary files /dev/null and b/public/images/how-wayheld-thinks/how-sub.webp differ
diff --git a/public/images/how-wayheld-thinks/how-wayheld-1.webp b/public/images/how-wayheld-thinks/how-wayheld-1.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/how-wayheld-thinks/how-wayheld-1.webp differ
diff --git a/public/images/how-wayheld-thinks/how-wayheld-2.webp b/public/images/how-wayheld-thinks/how-wayheld-2.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/how-wayheld-thinks/how-wayheld-2.webp differ
diff --git a/public/images/journeys/journey-1.webp b/public/images/journeys/journey-1.webp
new file mode 100644
index 0000000..21cc7db
Binary files /dev/null and b/public/images/journeys/journey-1.webp differ
diff --git a/public/images/journeys/journey-2.webp b/public/images/journeys/journey-2.webp
new file mode 100644
index 0000000..785fc76
Binary files /dev/null and b/public/images/journeys/journey-2.webp differ
diff --git a/public/images/journeys/journey-3.webp b/public/images/journeys/journey-3.webp
new file mode 100644
index 0000000..246e13f
Binary files /dev/null and b/public/images/journeys/journey-3.webp differ
diff --git a/public/images/journeys/journey-4.webp b/public/images/journeys/journey-4.webp
new file mode 100644
index 0000000..e969456
Binary files /dev/null and b/public/images/journeys/journey-4.webp differ
diff --git a/src/app/about/page.tsx b/src/app/about/page.tsx
index ab59377..ca04b78 100644
--- a/src/app/about/page.tsx
+++ b/src/app/about/page.tsx
@@ -12,7 +12,7 @@ export const metadata: Metadata = {
     siteName: "Wayheld",
     images: [
       {
-        url: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200",
+        url: "/images/about/about-og.webp",
         width: 1200,
         height: 630,
         alt: "Wayheld About Cover",
@@ -25,7 +25,7 @@ export const metadata: Metadata = {
     card: "summary_large_image",
     title: "About Wayheld — Our Philosophy",
     description: "Wayheld is a slow travel companion designed for intentional, regenerative journeys.",
-    images: ["https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200"],
+    images: ["/images/about/about-og.webp"],
   },
 };
 
@@ -33,20 +33,20 @@ const GUIDING_PRINCIPLES = [
   {
     title: "Travel Slowly",
     description: "Take time to truly experience each destination, allowing for spontaneous discoveries and meaningful connections. We believe the rush of checking boxes is the enemy of travel.",
-    image: "https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?auto=format&fit=crop&q=80&w=800",
+    image: "/images/about/about-feature-1.webp",
     alt: "Quiet misty forest trail",
   },
   {
     title: "Honor Heritage",
     description: "Appreciate the history and culture of every place, understanding the stories that shaped each destination. We center the voices of the people who live there.",
-    image: "https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&q=80&w=800",
+    image: "/images/about/about-feature-2.webp",
     alt: "Artisan working on traditional pottery",
     reverse: true,
   },
   {
     title: "Discover Beyond",
     description: "Venture beyond common tourist routes to find authentic experiences and hidden cultural gems. It’s the quiet cafes and narrow alleys that leave the longest impression.",
-    image: "https://images.unsplash.com/photo-1433086966358-54859d0ed716?auto=format&fit=crop&q=80&w=800",
+    image: "/images/about/about-feature-3.webp",
     alt: "Small cobblestone village street",
   },
 ];
@@ -58,7 +58,7 @@ export default function AboutPage() {
         <PageHero
           title="About Wayheld"
           subtitle="Wayheld means to be held by a place, to slow down enough that somewhere can actually change you. That’s what we’ve always believed travel could be."
-          image="https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?auto=format&fit=crop&q=80&w=2000"
+          image="/images/about/about-hero.webp"
           alt="Majestic quiet mountain valley"
           kicker="Our Story"
         />
diff --git a/src/app/commitment/page.tsx b/src/app/commitment/page.tsx
index 1a29d8d..357a991 100644
--- a/src/app/commitment/page.tsx
+++ b/src/app/commitment/page.tsx
@@ -12,7 +12,7 @@ export const metadata: Metadata = {
     siteName: "Wayheld",
     images: [
       {
-        url: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200",
+        url: "/images/commitment/commitment-og.webp",
         width: 1200,
         height: 630,
         alt: "Wayheld Commitment",
@@ -27,20 +27,20 @@ const COMMITMENTS = [
   {
     title: "The Art of Slow Travel",
     description: "Speed destroys the subtle textures of experience. When we rush through places, we miss the quiet conversations, the hidden pathways, and the gentle rhythms that define a destination's true character. Slow travel is not about taking more time, it's about being present to the time you have.",
-    image: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=800",
+    image: "/images/commitment/commitment-feature-1.webp",
     alt: "Traveler standing overlooking a vast misty mountain range",
   },
   {
     title: "Honoring Cultural Heritage",
     description: "Every destination carries the weight of history, the wisdom of traditions, and the hopes of its people. Respectful travel means approaching these places as a gracious guest, not a consumer of someone else's heritage. At Wayheld, that responsibility runs through every decision we make.",
-    image: "https://images.unsplash.com/photo-1494548162494-384bba4ab999?auto=format&fit=crop&q=80&w=800",
+    image: "/images/commitment/commitment-feature-2.webp",
     alt: "Local market in an old town",
     reverse: true,
   },
   {
     title: "Discovering Meaningful Routes",
     description: "The path between destinations can be as transformative as the destinations themselves. We believe in routes that reveal a landscape's character, connect communities, and tell stories through geography. These are not the fastest routes, they are the most honest ones.",
-    image: "https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?auto=format&fit=crop&q=80&w=800",
+    image: "/images/commitment/commitment-feature-3.webp",
     alt: "Small village nestled in green hills",
   },
 ];
@@ -52,7 +52,7 @@ export default function CommitmentPage() {
         <PageHero
           title="Commitment"
           subtitle="We encourage travelers to move through places with care, curiosity, and cultural respect. These are standards we share."
-          image="https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?auto=format&fit=crop&q=80&w=2000"
+          image="/images/commitment/commitment-hero.webp"
           alt="Mist rolling over a dense green forest"
           kicker="Our Promise"
         />
diff --git a/src/app/contact/page.tsx b/src/app/contact/page.tsx
index 4b379fa..f00eb48 100644
--- a/src/app/contact/page.tsx
+++ b/src/app/contact/page.tsx
@@ -12,7 +12,7 @@ export const metadata: Metadata = {
     siteName: "Wayheld",
     images: [
       {
-        url: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200",
+        url: "/images/contact/contact-og.webp",
         width: 1200,
         height: 630,
         alt: "Wayheld Contact",
@@ -30,7 +30,7 @@ export default function ContactPage() {
         <PageHero
           title="Contact Us"
           subtitle="Whether you have a question about our journeys, want to partner with us, or just want to say hello, we are here."
-          image="https://images.unsplash.com/photo-1490682143684-14369e18dce8?auto=format&fit=crop&q=80&w=2000"
+          image="/images/contact/contact-hero.webp"
           alt="Vintage writing desk overlooking a garden"
           kicker="Reach Out"
         />
diff --git a/src/app/experiences/page.tsx b/src/app/experiences/page.tsx
index f5fd88e..933f813 100644
--- a/src/app/experiences/page.tsx
+++ b/src/app/experiences/page.tsx
@@ -12,7 +12,7 @@ export const metadata: Metadata = {
     siteName: "Wayheld",
     images: [
       {
-        url: "https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?auto=format&fit=crop&q=80&w=1200",
+        url: "/images/experiences/experiences-og.webp",
         width: 1200,
         height: 630,
         alt: "Wayheld Experiences",
@@ -27,20 +27,20 @@ const EXPERIENCES = [
   {
     title: "Cultural Immersion",
     description: "Step beyond tourist attractions and engage with local communities, traditions, and ways of life. Our cultural experiences connect you with artisans, storytellers, and locals who share their heritage and perspectives.",
-    image: "https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&q=80&w=800",
+    image: "/images/experiences/experiences-feature-1.webp",
     alt: "Artisan working in a traditional workshop",
   },
   {
     title: "Historical Discovery",
     description: "Uncover the layers of history that shape destinations today. Our historical experiences go beyond guidebook facts to reveal the human stories, conflicts, and triumphs that have shaped places over time. Walk ancient paths with local historians.",
-    image: "https://images.unsplash.com/photo-1490682143684-14369e18dce8?auto=format&fit=crop&q=80&w=800",
+    image: "/images/experiences/experiences-feature-2.webp",
     alt: "Ancient stone pathway in a remote village",
     reverse: true,
   },
   {
     title: "Mindful Exploration",
     description: "Embrace slow travel that prioritizes depth over breadth. Our mindful exploration experiences encourage you to spend quality time in fewer places, allowing for genuine understanding and connection. Contemplative walks through landscapes and quiet reflection.",
-    image: "https://images.unsplash.com/photo-1433086966358-54859d0ed716?auto=format&fit=crop&q=80&w=800",
+    image: "/images/experiences/experiences-feature-3.webp",
     alt: "Solitary figure walking through a quiet misty valley",
   },
 ];
@@ -52,7 +52,7 @@ export default function ExperiencesPage() {
         <PageHero
           title="Experiences"
           subtitle="Discover meaningful travel experiences that foster deeper connections with places, culture, and history. Moving beyond surface-level tourism to create lasting memories."
-          image="https://images.unsplash.com/photo-1472214103451-9374bd1c798e?auto=format&fit=crop&q=80&w=2000"
+          image="/images/experiences/experiences-hero.webp"
           alt="Peaceful sunset over rolling hills"
           kicker="Journey Deeply"
         />
diff --git a/src/components/hero/ResilientImage.tsx b/src/components/hero/ResilientImage.tsx
index 49f9b98..5d356ec 100644
--- a/src/components/hero/ResilientImage.tsx
+++ b/src/components/hero/ResilientImage.tsx
@@ -78,11 +78,11 @@ export function ResilientImage({
   }
 
   return (
-    <div className={`relative overflow-hidden bg-brand-border/30 ${rest.fill ? "absolute inset-0 w-full h-full" : ""} ${className ?? ""}`}>
+    <div className={`relative overflow-hidden bg-[#74876B] ${rest.fill ? "absolute inset-0 w-full h-full" : ""} ${className ?? ""}`}>
       <Image
         src={sources[sourceIndex]}
         alt={alt}
-        className={`transition-opacity duration-700 ease-in-out ${isLoaded ? "opacity-100" : "opacity-0"} ${rest.fill ? "object-cover" : ""}`}
+        className={`transition-opacity duration-300 ease-in-out ${isLoaded ? "opacity-100" : "opacity-0"} ${rest.fill ? "object-cover" : ""}`}
         onError={handleError}
         onLoad={handleLoad}
         {...rest}
diff --git a/src/components/hero/hero.config.ts b/src/components/hero/hero.config.ts
index 3653c3e..7ed800a 100644
--- a/src/components/hero/hero.config.ts
+++ b/src/components/hero/hero.config.ts
@@ -44,7 +44,7 @@ export const SHOWCASE: ShowcaseDestination[] = [
     tags: ["9 Days", "Slow Travel", "Local Hosts", "Heritage Route"],
     caption:
       "Tea houses, moss gardens and the discipline of stillness — walked, not rushed.",
-    image: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/hero/hero-1.webp",
     alt: "Cinematic golden hour shot of Yasaka Pagoda in Kyoto",
     atmosphere: {
       primary: "rgba(116, 135, 107, 0.20)",
@@ -68,7 +68,7 @@ export const SHOWCASE: ShowcaseDestination[] = [
     tags: ["6 Days", "Luxury Lakeside", "Private Boat", "Curated Journey"],
     caption:
       "Lakeside villas, quiet shores, and passenger ferries set to the speed of the water.",
-    image: "https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/hero/hero-2.webp",
     alt: "Luxury lakeside villa on Lake Como at sunset",
     atmosphere: {
       primary: "rgba(116, 135, 107, 0.18)",
@@ -92,7 +92,7 @@ export const SHOWCASE: ShowcaseDestination[] = [
     tags: ["7 Days", "Cinematic Skies", "Cave Suites", "Sunrise Trails"],
     caption:
       "Wind-sculpted stone canyons, cave dwellings, and quiet hot air balloon trails at dawn.",
-    image: "https://images.unsplash.com/photo-1501785888041-af3ef285b470?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/hero/hero-3.webp",
     alt: "Close-up cinematic shot of hot air balloons at sunrise in Cappadocia",
     atmosphere: {
       primary: "rgba(116, 135, 107, 0.15)",
@@ -116,7 +116,7 @@ export const SHOWCASE: ShowcaseDestination[] = [
     tags: ["5 Days", "Slow Food", "Vineyard Stay", "Private Chef"],
     caption:
       "Rolling hills, centuries-old olive groves, and long dinners set under the vineyard stars.",
-    image: "https://images.unsplash.com/photo-1490682143684-14369e18dce8?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/hero/hero-4.webp",
     alt: "Tuscany vineyard dinner scene at dusk",
     atmosphere: {
       primary: "rgba(116, 135, 107, 0.18)",
@@ -140,7 +140,7 @@ export const SHOWCASE: ShowcaseDestination[] = [
     tags: ["8 Days", "Rail Journey", "Eco-Travel", "Mountain Chalets"],
     caption:
       "Deep valleys, seventy-two waterfalls, and the quiet creak of mountain railways.",
-    image: "https://images.unsplash.com/photo-1433086966358-54859d0ed716?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/hero/hero-5.webp",
     alt: "Swiss alpine rail journey through snow-capped mountains",
     atmosphere: {
       primary: "rgba(116, 135, 107, 0.22)",
diff --git a/src/components/sections/HowWayheldThinks.tsx b/src/components/sections/HowWayheldThinks.tsx
index fbcc8e2..c7105e7 100644
--- a/src/components/sections/HowWayheldThinks.tsx
+++ b/src/components/sections/HowWayheldThinks.tsx
@@ -1,6 +1,11 @@
 "use client";
 
-import { motion, useReducedMotion, useScroll, useTransform } from "motion/react";
+import {
+  motion,
+  useReducedMotion,
+  useScroll,
+  useTransform,
+} from "motion/react";
 import { useRef } from "react";
 import Image from "next/image";
 import { Section } from "@/components/ui";
@@ -49,7 +54,124 @@ function VoiceMark({ voice }: { voice: ThinkingStage["voice"] }) {
   }
 }
 
-function Stage({ stage, index }: { stage: ThinkingStage; index: number }) {
+/** 
+ * Exact copy of the original premium collage layout, extracted into a reusable component.
+ * It strictly maintains the same overlapping images, quote cards, shadows, and spacing.
+ */
+function EditorialCollage({
+  mainImg,
+  mainTag,
+  mainTitle,
+  subImg,
+  subTag,
+  subTitle,
+  quote,
+  author,
+  detail
+}: {
+  mainImg: string;
+  mainTag: string;
+  mainTitle: string;
+  subImg: string;
+  subTag: string;
+  subTitle: string;
+  quote: string;
+  author: string;
+  detail: string;
+}) {
+  return (
+    <div className="relative h-[550px] w-full lg:h-[680px]">
+      {/* Main large image */}
+      <div className="absolute right-0 top-0 h-[360px] w-[90%] overflow-hidden rounded-[2rem] shadow-[0_30px_60px_-15px_rgba(51,51,47,0.3)] lg:h-[460px] lg:w-[85%] lg:rounded-[2.5rem]">
+        <Image
+          src={mainImg}
+          alt={mainTitle}
+          fill
+          sizes="(max-width: 1024px) 90vw, 55vw"
+          className="object-cover transition-transform duration-[2s] hover:scale-105"
+        />
+        <div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/60 via-black/10 to-transparent" />
+        <div className="absolute bottom-6 left-6 text-[#F4EFE6] lg:bottom-8 lg:left-8">
+          <span className="text-[0.6rem] font-bold uppercase tracking-[0.2em] text-[#A8A69D] lg:text-[0.65rem]">
+            {mainTag}
+          </span>
+          <h4 className="mt-1 font-display text-xl lg:mt-2 lg:text-2xl">{mainTitle}</h4>
+        </div>
+      </div>
+
+      {/* Overlapping secondary image */}
+      <div className="absolute left-0 top-[220px] z-10 h-[280px] w-[55%] overflow-hidden rounded-3xl shadow-[0_20px_40px_-10px_rgba(51,51,47,0.2)] lg:top-[300px] lg:h-[360px] lg:w-[50%]">
+        <Image
+          src={subImg}
+          alt={subTitle}
+          fill
+          sizes="(max-width: 1024px) 50vw, 30vw"
+          className="object-cover transition-transform duration-[2s] hover:scale-105"
+        />
+        <div className="pointer-events-none absolute inset-0 bg-gradient-to-t from-black/70 via-transparent to-transparent" />
+        <div className="absolute bottom-5 left-5 text-[#F4EFE6] lg:bottom-6 lg:left-6">
+          <span className="text-[0.5rem] font-bold uppercase tracking-[0.16em] text-[#A8A69D] lg:text-[0.55rem]">
+            {subTag}
+          </span>
+          <p className="mt-1 text-xs font-medium lg:text-sm">{subTitle}</p>
+        </div>
+      </div>
+
+      {/* Floating Quote Box */}
+      <div className="absolute bottom-0 right-0 z-20 w-[85%] rounded-3xl border border-[#D8D2C8] bg-white/95 p-6 shadow-2xl backdrop-blur-xl lg:bottom-8 lg:right-6 lg:w-[48%] lg:bg-white/90 lg:p-8">
+        <blockquote className="font-display text-base italic leading-relaxed text-[#33332F] lg:text-lg">
+          “{quote}”
+        </blockquote>
+        <div className="mt-4 flex items-center gap-3 lg:mt-6 lg:gap-4">
+          <span className="h-px w-6 bg-[#74876B] lg:w-8" />
+          <p className="text-[0.6rem] font-bold uppercase tracking-widest text-[#74876B] lg:text-[0.65rem]">
+            {author}
+          </p>
+        </div>
+        <p className="mt-4 text-xs leading-relaxed text-[#504F4A] lg:mt-5">
+          {detail}
+        </p>
+      </div>
+    </div>
+  );
+}
+
+const COLLAGES = [
+  <EditorialCollage 
+    key="0"
+    mainImg="/images/how-wayheld-thinks/how-main.webp"
+    mainTag="Cairngorms"
+    mainTitle="Hidden valleys"
+    subImg="/images/how-wayheld-thinks/how-wayheld-1.webp"
+    subTag="Ancient pine"
+    subTitle="Deep forests"
+    quote="The eastern valleys are entirely overlooked by standard routing algorithms."
+    author="Wayheld AI"
+    detail="Millions of data points cross-referenced to find the exact atmospheric match for your pace."
+  />,
+  <EditorialCollage 
+    key="1"
+    mainImg="/images/how-wayheld-thinks/how-wayheld-2.webp"
+    mainTag="Fife Coastline"
+    mainTitle="Harbour cafés"
+    subImg="/images/how-wayheld-thinks/how-sub.webp"
+    subTag="Local craft"
+    subTitle="Quiet mornings"
+    quote="The fishing villages of Fife are quiet, and the harbour cafés keep their own hours."
+    author="Local Guide"
+    detail="Wayheld designs around context, routing you past St Monans and Crail for heritage stays."
+  />
+];
+
+function Stage({ 
+  stage, 
+  index,
+  visual
+}: { 
+  stage: ThinkingStage; 
+  index: number; 
+  visual?: React.ReactNode;
+}) {
   const isWayheld = stage.voice === "wayheld";
   const accent = isWayheld || stage.voice === "route";
 
@@ -72,7 +194,7 @@ function Stage({ stage, index }: { stage: ThinkingStage; index: number }) {
         <VoiceMark voice={stage.voice} />
       </span>
 
-      <div className="pb-14 sm:pb-16">
+      <div className="pb-12 sm:pb-16 lg:pb-32">
         <p
           className={`text-[0.7rem] font-medium uppercase tracking-[0.22em] ${
             accent ? "text-brand-btn-primary" : "text-brand-text-secondary"
@@ -108,14 +230,21 @@ function Stage({ stage, index }: { stage: ThinkingStage; index: number }) {
           </ul>
         )}
       </div>
+
+      {/* Mobile Visual Flow (Interspersed) */}
+      {visual && (
+        <div className="block w-full pb-16 lg:hidden">
+          {visual}
+        </div>
+      )}
     </motion.li>
   );
 }
 
 /**
  * "How Wayheld Thinks" — a scroll-driven narrative proving Wayheld reasons
- * rather than chats. A vertical spine connects four beats of thought, with a
- * progress line that draws itself as the reader scrolls.
+ * rather than chats. Uses a natural scrolling column of repeated editorial
+ * collages to eliminate whitespace and maintain exactly the same premium design language.
  */
 export function HowWayheldThinks() {
   const reduceMotion = useReducedMotion();
@@ -128,12 +257,7 @@ export function HowWayheldThinks() {
   const lineScale = useTransform(scrollYProgress, [0, 1], [0, 1]);
 
   return (
-    <Section
-      id="how"
-      labelledBy="how-heading"
-
-    >
-      <>
+    <Section id="how" labelledBy="how-heading">
       <div className="grid grid-cols-1 gap-8 lg:grid-cols-12 lg:gap-12">
         {/* Left Column: Title + Intro + Reasoning spine */}
         <div className="lg:col-span-5">
@@ -168,77 +292,47 @@ export function HowWayheldThinks() {
             <motion.span
               aria-hidden
               style={{ scaleY: reduceMotion ? 1 : lineScale }}
-              className="absolute bottom-0 left-3 top-1.5 w-px origin-top bg-linear-to-b from-[#74876B] via-[#74876B]/60 to-transparent sm:left-4"
+              className="absolute bottom-0 left-3 top-1.5 w-px origin-top bg-gradient-to-b from-[#74876B] via-[#74876B]/60 to-transparent sm:left-4"
             />
 
             <ol className="relative">
-              {THINKING_STAGES.map((stage, index) => (
-                <Stage key={stage.label} stage={stage} index={index} />
-              ))}
+              {THINKING_STAGES.map((stage, index) => {
+                let visual = null;
+                // Intersperse visual 0 after stage 1, and visual 1 after stage 3
+                if (index === 1) visual = COLLAGES[0];
+                if (index === 3) visual = COLLAGES[1];
+
+                return (
+                  <Stage 
+                    key={stage.label} 
+                    stage={stage} 
+                    index={index} 
+                    visual={visual}
+                  />
+                );
+              })}
             </ol>
           </div>
         </div>
 
-        {/* Right Column: Premium Editorial Travel Imagery (Sticky collage) */}
-        <div className="hidden lg:col-span-7 lg:block">
-          <div className="sticky top-32 pl-8">
-            <div className="relative h-[680px] w-full">
-              {/* Main large image */}
-              <div className="absolute top-0 right-0 h-[460px] w-[85%] overflow-hidden rounded-[2.5rem] shadow-[0_30px_60px_-15px_rgba(51,51,47,0.3)]">
-                <Image
-                  src="https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200"
-                  alt="Misty cliffs and castle ruins in St Andrews, Scotland"
-                  fill
-                  sizes="55vw"
-                  className="object-cover transition-transform duration-[2s] hover:scale-105"
-                />
-                <div className="absolute inset-0 bg-linear-to-t from-black/60 via-black/10 to-transparent pointer-events-none" />
-                <div className="absolute bottom-8 left-8 text-[#F4EFE6]">
-                  <span className="text-[0.65rem] font-bold uppercase tracking-[0.2em] text-[#A8A69D]">
-                    Aberdeenshire cliffs
-                  </span>
-                  <h4 className="mt-2 font-display text-2xl">The end of the Neuk path</h4>
-                </div>
-              </div>
-
-              {/* Overlapping secondary image */}
-              <div className="absolute top-[300px] left-0 z-10 h-[360px] w-[50%] overflow-hidden rounded-3xl shadow-[0_20px_40px_-10px_rgba(51,51,47,0.2)]">
-                <Image
-                  src="https://images.unsplash.com/photo-1433086966358-54859d0ed716?auto=format&fit=crop&q=80&w=800"
-                  alt="Historic quiet harbour in Fife, Scotland"
-                  fill
-                  sizes="30vw"
-                  className="object-cover transition-transform duration-[2s] hover:scale-105"
-                />
-                <div className="absolute inset-0 bg-linear-to-t from-black/70 via-transparent to-transparent pointer-events-none" />
-                <div className="absolute bottom-6 left-6 text-[#F4EFE6]">
-                  <span className="text-[0.55rem] font-bold uppercase tracking-[0.16em] text-[#A8A69D]">
-                    Fife coastline
-                  </span>
-                  <p className="mt-1 text-sm font-medium">Fishing villages</p>
-                </div>
-              </div>
-
-              {/* Floating Quote Box */}
-              <div className="absolute bottom-8 right-6 z-20 w-[48%] rounded-3xl border border-[#D8D2C8] bg-white/90 p-8 shadow-2xl backdrop-blur-xl">
-                <blockquote className="font-display text-lg italic leading-relaxed text-[#33332F]">
-                  “The fishing villages of Fife are quiet, and the harbour cafés keep their own hours.”
-                </blockquote>
-                <div className="mt-6 flex items-center gap-4">
-                  <span className="h-px w-8 bg-[#74876B]" />
-                  <p className="text-[0.65rem] font-bold uppercase tracking-widest text-[#74876B]">
-                    Local Guide
-                  </p>
-                </div>
-                <p className="mt-5 text-xs leading-relaxed text-[#504F4A]">
-                  Wayheld designs around context, routing you past St Monans and Crail for heritage stays and local stories.
-                </p>
-              </div>
-            </div>
+        {/* Right Column: Long Editorial Visual Cascade (Natural Scroll) */}
+        <div className="hidden lg:block lg:col-span-7">
+          {/* Matches the top offset of the spineRef (mt-8 lg:mt-10) to perfectly align visually */}
+          <div className="flex flex-col gap-[200px] pl-8 mt-8 lg:mt-10">
+            {COLLAGES.map((collage, i) => (
+              <motion.div 
+                key={i}
+                initial={{ opacity: 0, y: 24 }}
+                whileInView={{ opacity: 1, y: 0 }}
+                viewport={{ once: true, amount: 0.2 }}
+                transition={{ duration: 0.85, ease: EASE_EXPO }}
+              >
+                {collage}
+              </motion.div>
+            ))}
           </div>
         </div>
       </div>
-    </>
     </Section>
   );
 }
diff --git a/src/components/sections/PageHero.tsx b/src/components/sections/PageHero.tsx
index 6b09e91..71383d9 100644
--- a/src/components/sections/PageHero.tsx
+++ b/src/components/sections/PageHero.tsx
@@ -15,7 +15,7 @@ interface PageHeroProps {
 
 export function PageHero({ title, subtitle, image, alt, kicker }: PageHeroProps) {
   return (
-    <section className="relative isolate flex h-[80vh] min-h-[600px] w-full flex-col overflow-hidden bg-brand-bg">
+    <section className="relative isolate flex h-[80vh] min-h-[600px] w-full flex-col overflow-hidden bg-[#74876B]">
       <div className="absolute inset-0 z-0">
         <ResilientImage
           src={image}
diff --git a/src/components/sections/StatisticsSection.tsx b/src/components/sections/StatisticsSection.tsx
index 0a9e3bf..cddecc8 100644
--- a/src/components/sections/StatisticsSection.tsx
+++ b/src/components/sections/StatisticsSection.tsx
@@ -1,8 +1,8 @@
 "use client";
 
-import { motion } from "motion/react";
-import { Section } from "@/components/ui";
-import { containerVariants, riseVariants, EASE_EXPO } from "@/lib/design";
+import { motion, useInView, useReducedMotion } from "motion/react";
+import { useEffect, useRef, useState } from "react";
+import { containerVariants, riseVariants } from "@/lib/design";
 
 const STATS = [
   { value: "120+", label: "Slow Regions" },
@@ -11,36 +11,82 @@ const STATS = [
   { value: "4", label: "Travel Styles" },
 ];
 
+function AnimatedCounter({ value }: { value: string }) {
+  const hasPlus = value.includes("+");
+  const numValue = parseInt(value.replace(/\D/g, ""), 10);
+  
+  const ref = useRef<HTMLSpanElement>(null);
+  const isInView = useInView(ref, { once: true, amount: 0.5 });
+  const reduceMotion = useReducedMotion();
+  const [displayValue, setDisplayValue] = useState(reduceMotion ? numValue : 0);
+
+  useEffect(() => {
+    if (reduceMotion) return;
+    if (isInView) {
+      let animationFrame: number;
+      const duration = 1800;
+      const startTime = performance.now();
+
+      const step = (currentTime: number) => {
+        const elapsed = currentTime - startTime;
+        const progress = Math.min(elapsed / duration, 1);
+        
+        // easeOutQuint
+        const easeProgress = 1 - Math.pow(1 - progress, 5);
+        
+        setDisplayValue(Math.floor(easeProgress * numValue));
+        
+        if (progress < 1) {
+          animationFrame = requestAnimationFrame(step);
+        }
+      };
+      animationFrame = requestAnimationFrame(step);
+      return () => cancelAnimationFrame(animationFrame);
+    }
+  }, [isInView, numValue, reduceMotion]);
+
+  return (
+    <span ref={ref} className="tabular-nums">
+      {displayValue}{hasPlus ? "+" : ""}
+    </span>
+  );
+}
+
+function StatisticCard({ stat }: { stat: { value: string; label: string } }) {
+  return (
+    <motion.div
+      variants={riseVariants}
+      className="group relative flex h-full w-full flex-col items-center justify-center text-center px-4 py-8 sm:p-10 rounded-2xl bg-[#F4EFE6] shadow-[0_8px_30px_rgba(0,0,0,0.06)] border border-[rgba(51,51,47,0.04)] lg:transition-all lg:duration-250 lg:ease-out lg:hover:-translate-y-1 lg:hover:shadow-[0_16px_40px_rgba(0,0,0,0.12)]"
+    >
+      <span className="font-display text-4xl min-[400px]:text-5xl sm:text-6xl text-[#33332F] font-light tracking-tight">
+        <AnimatedCounter value={stat.value} />
+      </span>
+      <div className="mt-4 sm:mt-5 flex flex-col items-center">
+        <span className="mb-3 sm:mb-4 h-px w-6 bg-[#74876B]/30 transition-all duration-300 lg:group-hover:w-10 lg:group-hover:bg-[#74876B]/60" aria-hidden />
+        <span className="text-[0.6rem] sm:text-[0.65rem] uppercase tracking-[0.2em] text-[#74876B] font-bold">
+          {stat.label}
+        </span>
+      </div>
+    </motion.div>
+  );
+}
+
 export function StatisticsSection() {
   return (
     <section
       id="statistics"
-      className="relative isolate w-full overflow-hidden bg-[#74876B] py-16 text-[#F4EFE6] sm:py-20 lg:py-24 border-y border-[rgba(244,239,230,0.15)]"
+      className="relative isolate w-full bg-[#74876B] py-16 sm:py-20 lg:py-24 border-y border-[rgba(244,239,230,0.15)]"
     >
       <motion.div
         variants={containerVariants}
         initial="hidden"
         whileInView="show"
-        viewport={{ once: true, amount: 0.3 }}
+        viewport={{ once: true, amount: 0.2 }}
         className="mx-auto w-full max-w-7xl px-5 sm:px-6 lg:px-10"
       >
-        <div className="grid grid-cols-2 gap-8 md:grid-cols-4 lg:gap-12">
-          {STATS.map((stat, index) => (
-            <motion.div
-              key={stat.label}
-              variants={riseVariants}
-              className="flex flex-col items-center text-center sm:items-start sm:text-left"
-            >
-              <span className="font-display text-5xl sm:text-6xl lg:text-7xl text-[#F4EFE6] font-light tracking-tight">
-                {stat.value}
-              </span>
-              <div className="mt-4 flex flex-col items-center sm:items-start">
-                <span className="mb-3 h-px w-6 bg-[rgba(244,239,230,0.3)]" aria-hidden />
-                <span className="text-[0.65rem] uppercase tracking-[0.2em] text-[rgba(244,239,230,0.95)] font-bold">
-                  {stat.label}
-                </span>
-              </div>
-            </motion.div>
+        <div className="grid grid-cols-1 min-[480px]:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6 lg:gap-8">
+          {STATS.map((stat) => (
+            <StatisticCard key={stat.label} stat={stat} />
           ))}
         </div>
       </motion.div>
diff --git a/src/components/sections/journeys.config.ts b/src/components/sections/journeys.config.ts
index 5f7666a..5562e66 100644
--- a/src/components/sections/journeys.config.ts
+++ b/src/components/sections/journeys.config.ts
@@ -39,7 +39,7 @@ export const JOURNEYS: Journey[] = [
     summary:
       "Harbour mornings and empty cliffs, where the North Sea light belongs only to you.",
     highlights: ["Coastal heritage", "Low footfall", "Rail-reachable"],
-    image: "https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/journeys/journey-1.webp",
     alt: "Rugged cliffs meeting the North Sea on the east coast of Scotland",
   },
   {
@@ -51,7 +51,7 @@ export const JOURNEYS: Journey[] = [
     summary:
       "Cedar forests, tea houses and temple paths walked at the pace of breath.",
     highlights: ["Heritage-rich", "Walkable", "Crowd-free corners"],
-    image: "https://images.unsplash.com/photo-1447752875215-b2761acb3c5d?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/journeys/journey-2.webp",
     alt: "Misty temple pathway lined with maple trees in Kyoto",
   },
   {
@@ -63,7 +63,7 @@ export const JOURNEYS: Journey[] = [
     summary:
       "Hanging glaciers, marble caves and the deep quiet that only long stays reveal.",
     highlights: ["Long stays", "Regenerative lodges", "Local guides"],
-    image: "https://images.unsplash.com/photo-1464822759023-fed622ff2c3b?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/journeys/journey-3.webp",
     alt: "Granite peaks reflected in a still lake in Patagonia",
   },
   {
@@ -75,7 +75,7 @@ export const JOURNEYS: Journey[] = [
     summary:
       "Dawn paddles through narrow canals and kitchens that open their doors to you.",
     highlights: ["Community homestays", "Slow mornings", "Plastic-free"],
-    image: "https://images.unsplash.com/photo-1494548162494-384bba4ab999?auto=format&fit=crop&q=80&w=1200",
+    image: "/images/journeys/journey-4.webp",
     alt: "Traditional houseboat gliding through the Kerala backwaters at dusk",
   },
 ];
